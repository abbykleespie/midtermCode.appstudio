/*
function calcAvgSquare(num1, num2, num3) {
 let sqNum1 = Math.pow(num1, 2)
 let sum = num1 + num2 + num3
 let avg = (sum / 3) * sqNum1
 return(avg)
}

function divideTwo(div1, div2) {
  return(div1 % div2)
}

let whichFunction = prompt("Do you want an average calculation or a remainder? Type: average or remainder")

if (whichFunction == "average") {
  console.log(`The answer is ${calcAvgSquare(input1, input2, input3)}`)
} else {
  console.log(`The result of dividing ${input1} by ${input2} is ${divideTwo(input1, input2, input3)}`)
}
*/
