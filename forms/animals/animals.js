/*
let myAnimals = ['dog', 'cat', 'horse', 'meerkat']
let addAnimal = prompt("What animal do you want to add to the list?")
let anyCase = addAnimal.toLowerCase()

myAnimals.push(anyCase)

console.log(`The last animal is a/an ${myAnimals[myAnimals.indexOf(anyCase)]}`)
*/
