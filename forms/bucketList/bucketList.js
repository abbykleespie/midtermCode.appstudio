/*
let ask = ''
let bucketList = []
let doneAsking = false

while (doneAsking == false) {
 ask = prompt("Do you want to see your bucket list, add an item, or stop? Type: Bucket list or Add item or Stop")
  if (ask == "Bucket list") {
    doneAsking = false
    for (let i = 0; i < bucketList.length; i++){
      console.log(bucketList[i])
    }
  }
  else if (ask == "Add item") {
    doneAsking = false
    newItem = prompt("Please enter a new item")
    bucketList.push(newItem)
    for (let i = 0; i < bucketList.length; i++){
      console.log(bucketList[i])
    }
  }
  else{
    doneAsking = true
  }
}
*/
