/*
let stop = 2

for (let run = 0; run < stop; run++){
  let newAnimal = prompt("What animal do you want to add to the list?")
  let lowerCase = newAnimal.toLowerCase()
  myAnimals.push(lowerCase)
  console.log(`The last animal is a/an ${myAnimals[myAnimals.indexOf(lowerCase)]}`)
}
*/